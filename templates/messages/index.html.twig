{% extends 'base.html.twig' %}

{% block title %}Messages{% endblock %}

{% block body %}
<div class="container animate-fade-in">
    <h1 class="mb-4 text-center"><i class="fas fa-envelope"></i> Messages</h1>
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0"><i class="fas fa-inbox"></i> Inbox</h5>
                </div>
                <div class="card-body">
                    <p class="text-muted">No messages at the moment.</p>
                    <p>This page will be used to display messages from donors and support requests.</p>
                </div>
            </div>
        </div>
    </div>
    <div class="text-center mt-4">
        <a href="{{ path('app_home') }}" class="btn btn-custom"><i class="fas fa-arrow-left"></i> Back to Dashboard</a>
    </div>
</div>
{% endblock %}